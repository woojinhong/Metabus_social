name: "Documentation validation"

on:
  pull_request:
    paths:
      - "**/*.md"
      - "AGENTS.md"
      - ".github/**"
      - "scripts/docs/**"
      - "src/main/resources/db/migration/**"
  push:
    paths:
      - "**/*.md"
      - "AGENTS.md"
      - ".github/**"
      - "scripts/docs/**"
      - "src/main/resources/db/migration/**"

permissions:
  contents: read

jobs:
  validate-docs:
    if: "${{ github.event_name == 'pull_request' || github.ref_name == github.event.repository.default_branch }}"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: "Checkout repository"
        uses: actions/checkout@v6
      - name: "Set up Node.js"
        uses: actions/setup-node@v6
        with:
          node-version: "22"
      - name: "Test semantic gates"
        run: node scripts/docs/semantic-gates.test.mjs
      - name: "Validate durable documentation"
        run: node scripts/docs/validate-docs.mjs
